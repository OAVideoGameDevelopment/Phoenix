extends Resource
class_name CharacterStats

@export var max_health := 100
@export var max_stamina := 100 # lowkey don't know if we're using stamin or not :sob:
@export var attack_power := 10
@export var defense := 2
@export var speed := 200
